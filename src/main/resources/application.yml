spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/storetable
    username: storetable
    password: 1111
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
